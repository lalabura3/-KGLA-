# ── Web framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ── Database ──
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# ── Redis / Celery ──
redis>=5.2.0
celery[redis]>=5.4.0

# ── Structured logging ──
structlog>=24.4.0

# ── HTTP client (for service-to-service calls) ──
httpx>=0.28.0

# ── ASR / audio ──
# (Whisper service is a separate container; this package only needs the client)

# ── LLM / AI ──
openai>=1.58.0

# ── File handling ──
python-multipart>=0.0.18
aiofiles>=24.1.0

# ── Auth (future) ──
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# ── Dev / Test ──
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.28.0  # TestClient
